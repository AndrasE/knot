export default function GuestlistSection() {
  return (
    <section
      id="guestlist"
      className="flex items-center justify-center h-screen">
      <div className="text-center">
        <h1 className="text-4xl font-bold">Guestlist Preview</h1>
        {/* Add guestlist-specific content here */}
      </div>
    </section>
  );
}
